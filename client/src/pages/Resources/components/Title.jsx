import React from 'react'

export default function Title({text}) {
    return (
        <div>
            <h1 className='font-bold text-[34px] font-title mt-[42px]'>{text}</h1>
        </div>
    )
}
